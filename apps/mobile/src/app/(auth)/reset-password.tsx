import { router } from 'expo-router'; import { StyleSheet, View } from 'react-native'; import { colors, spacing, typography } from '@/theme'; import { Text } from '@/components/ui/Text'; import { Input } from '@/components/ui/Input'; import { Button } from '@/components/ui/Button'; import { useState } from 'react';
export default function ResetPassword(){const [password,setPassword]=useState('');return <View style={styles.container}><Text style={styles.kicker}>NEW PASSWORD</Text><Text style={styles.title}>Choose something secure.</Text><Text style={styles.intro}>Your password should be unique to ORYN and difficult to guess.</Text><Input label="New password" value={password} onChangeText={setPassword} secureTextEntry placeholder="At least 8 characters"/><View style={{marginTop:spacing.xl}}><Button label="Update password" onPress={()=>router.replace('/(auth)/login')} disabled={password.length<8}/></View></View>}
const styles=StyleSheet.create({container:{flex:1,backgroundColor:colors.background,padding:spacing.xl,paddingTop:spacing.huge},kicker:{...typography.overline},title:{...typography.h1,fontSize:34,lineHeight:40,marginTop:spacing.md},intro:{...typography.body,color:colors.textSecondary,marginVertical:spacing.xxl,maxWidth:340}});
