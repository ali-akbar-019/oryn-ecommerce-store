import { useState } from 'react'; import { ScrollView, StyleSheet, View } from 'react-native'; import { router } from 'expo-router'; import { colors, spacing, typography } from '@/theme'; import { Text } from '@/components/ui/Text'; import { Input } from '@/components/ui/Input'; import { Button } from '@/components/ui/Button'; import { useAuthStore } from '@/store/authStore';
export default function EditProfile(){const user=useAuthStore(s=>s.user);const [name,setName]=useState(user?.name||'');return <ScrollView style={styles.screen} contentContainerStyle={styles.container}><Text style={styles.back} onPress={()=>router.back()}>‹ Back</Text><Text style={styles.kicker}>PROFILE</Text><Text style={styles.title}>Personal information</Text><Text style={styles.intro}>Keep your details current for a smoother checkout experience.</Text><View style={styles.form}><Input label="Full name" value={name} onChangeText={setName}/><Input label="Email" value={user?.email||''} editable={false}/></View><Button title="Save changes" onPress={()=>router.back()}/></ScrollView>}
const styles=StyleSheet.create({screen:{flex:1,backgroundColor:colors.background},container:{padding:spacing.xl,paddingTop:spacing.xxl},back:{...typography.body,color:colors.accent,marginBottom:spacing.xxxl},kicker:{...typography.overline},title:{...typography.h1,fontSize:32,lineHeight:38,marginTop:spacing.md},intro:{...typography.body,color:colors.textSecondary,marginTop:8,marginBottom:spacing.xxl},form:{gap:spacing.lg,marginBottom:spacing.xl}});
