import { Router } from 'express'; import { z } from 'zod'; import { prisma } from '@oryn/database'; import { asyncHandler, sendData } from '../../common/http'; import { requireAuth, type AuthRequest } from '../../middleware/auth';
const schema = z.object({ label: z.string().min(1).max(40), firstName: z.string().min(1).max(80), lastName: z.string().min(1).max(80), line1: z.string().min(1).max(180), line2: z.string().max(180).optional(), city: z.string().min(1).max(80), state: z.string().max(80).optional(), postalCode: z.string().min(2).max(20), country: z.string().min(2).max(80), phone: z.string().min(7).max(30), isDefault: z.boolean().default(false) });
export const addressesRouter = Router(); addressesRouter.use(requireAuth);
addressesRouter.get('/', asyncHandler(async (req,res)=>sendData(res, await prisma.address.findMany({where:{userId:(req as AuthRequest).user!.id},orderBy:[{isDefault:'desc'},{createdAt:'desc'}]}))));
addressesRouter.post('/', asyncHandler(async(req,res)=>{const userId=(req as AuthRequest).user!.id; const input=schema.parse(req.body); const address=await prisma.$transaction(async tx=>{if(input.isDefault) await tx.address.updateMany({where:{userId},data:{isDefault:false}}); return tx.address.create({data:{...input,userId}})}); sendData(res,address,201);}));
addressesRouter.patch('/:id', asyncHandler(async(req,res)=>{const userId=(req as AuthRequest).user!.id; const input=schema.partial().parse(req.body); const address=await prisma.address.findFirst({where:{id:req.params.id,userId}}); if(!address) return res.status(404).json({error:{code:'ADDRESS_NOT_FOUND',message:'Address not found.'}}); const updated=await prisma.$transaction(async tx=>{if(input.isDefault) await tx.address.updateMany({where:{userId},data:{isDefault:false}}); return tx.address.update({where:{id:address.id},data:input});}); sendData(res,updated);}));
addressesRouter.delete('/:id', asyncHandler(async(req,res)=>{await prisma.address.deleteMany({where:{id:req.params.id,userId:(req as AuthRequest).user!.id}});res.status(204).send();}));
