import { Router } from 'express'; import { prisma } from '@oryn/database'; import { asStr, asyncHandler, sendData } from '../../common/http.js'; import { requireAuth, type AuthRequest } from '../../middleware/auth.js';
export const notificationsRouter = Router(); notificationsRouter.use(requireAuth);
notificationsRouter.get('/', asyncHandler(async (req, res) => { const userId = (req as AuthRequest).user!.id; const [items, unread, preferences] = await Promise.all([prisma.notification.findMany({ where: { userId }, orderBy: { createdAt: 'desc' }, take: 50 }), prisma.notification.count({ where: { userId, readAt: null } }), prisma.notificationPreference.findUnique({ where: { userId } })]); sendData(res, { items, unread, preferences }); }));
notificationsRouter.patch('/:id/read', asyncHandler(async (req, res) => { const item = await prisma.notification.updateMany({ where: { id: asStr(req.params.id), userId: (req as AuthRequest).user!.id }, data: { readAt: new Date() } }); sendData(res, { updated: item.count }); }));
notificationsRouter.post('/read-all', asyncHandler(async (req, res) => { const result = await prisma.notification.updateMany({ where: { userId: (req as AuthRequest).user!.id, readAt: null }, data: { readAt: new Date() } }); sendData(res, { updated: result.count }); }));
