# ORYN Navigation

Customer routes are file-based through Expo Router.

Auth, tabs, shop, search, products, cart, checkout, orders, returns, notifications, profile, reviews and campaigns are separated into route groups.
