# ORYN UX Architecture

Primary customer navigation: Home, Shop, Wishlist, Cart, Profile.

The customer experience follows discovery → product evaluation → cart → checkout → order tracking. Product filtering is category/schema driven. Checkout is multi-step. Guest wishlist/cart behavior is supported. Admin is a separate web application.
